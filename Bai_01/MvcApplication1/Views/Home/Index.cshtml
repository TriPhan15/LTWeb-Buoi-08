@model IEnumerable<MvcApplication1.Models.THELOAITIN>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "ThemMoi")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IDLOAI)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TENTHELOAI)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDLOAI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TENTHELOAI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDLOAI  }) |
            @Html.ActionLink("Details", "Details", new { id = item.IDLOAI }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IDLOAI })
        </td>
    </tr>
}

</table>
